<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Liste des Offres</title>
    <style>
        /* Define your styles for the PDF here */
        .portfolio-container {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-between;
            margin: 0 auto;
            max-width: 1000px; /* Ajustez la largeur du conteneur selon vos besoins */
        }
        .portfolio-item {
            width: calc(33.33% - 20px); /* Ajustez la largeur des cartes selon vos besoins */
            margin-bottom: 20px;
            border: 1px solid #ccc;
            padding: 10px;
            box-sizing: border-box;
            background-color: #f9f9f9; /* Couleur de fond */
        }
        .portfolio-item h2 {
            margin-top: 0;
            color: #333; /* Couleur du texte */
        }
        .portfolio-item p {
            margin-bottom: 5px;
            color: #666; /* Couleur du texte */
        }
        .portfolio-links {
            text-align: center;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="row portfolio-container" data-aos="fade-up">
        <center><h1 style="color: #333;">Liste des Offres</h1></center>
            {% for o in offres %}
                <div class="portfolio-item">
                    <div class="portfolio-wrap">
                        <div class="portfolio-links">
                            <h2 style="color: #000;">{{ o.idProduitOffre.nomProduit }}</h2>
                            <p>{{ o.description }}</p>
                            <p style="color: #888;">Date de début : {{ o.dateDebut ? o.dateDebut|date('Y-m-d') : '' }}</p>
                            <p style="color: #888;">Date de fin : {{ o.dateFin ? o.dateFin|date('Y-m-d') : '' }}</p>
                            <p>Prix : {{ o.idProduitOffre.prix * (1-(o.montantRemise / 100)) }}</p>
                        </div>
                    </div>
                </div>
                {% if loop.index % 3 == 0 %}
                    </div><div class="row portfolio-container" data-aos="fade-up">
                {% endif %}
            {% endfor %}
        </div>
    </div>
</body>
</html>
