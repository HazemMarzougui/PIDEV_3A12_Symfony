<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reset Password</title>
	<!-- Bootstrap CDN -->
	<link rel="stylesheet" href="{{ asset('css/login.css') }}">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-KyZXEAg3QhqLMpG8r+Knujsl5+3/z5GtT98nq+8MPd2I5TUkZjPk5O3hMhXfFJGa" crossorigin="anonymous">
    <!-- Font Awesome CDN -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet">
    <!-- Bootstrap Icons CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        /* CSS styles for the reset password section */
        .middle {
            background-color: #f2f7ff;
            padding: 50px 0;
            text-align: center;
        }

        .abt_caption {
            max-width: 600px;
            margin: auto;
        }

        .abt_caption h2 {
            font-size: 2rem;
            margin-bottom: 20px;
        }

        .abt_caption p {
            font-size: 1rem;
            line-height: 1.5;
            margin-bottom: 20px;
        }

        .abt_caption a {
            color: #F63C58;
            text-decoration: none;
        }

        .abt_caption a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
  <div class="screen screen-1 d-flex justify-content-center align-items-center">
<img src="https://img.hotimg.com/logoe0577f94706fddae.png" alt="Logo" class="logo">
<section class="middle">
    <div class="container">
        <div class="row align-items-center justify-content-between">
            <div class="col-xl-11 col-lg-12 col-md-6 col-sm-12">
                <div class="abt_caption">
                    <h2 class="ft-medium mb-4">Reset Password</h2>
<p class="mb-4">{{ message }}</p>
                </div>
            </div>
        </div>
    </div>
</section>

</body>
</html>
