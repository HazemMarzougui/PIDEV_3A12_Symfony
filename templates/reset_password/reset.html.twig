{# Your Twig template file #}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Change Password</title>
    <!-- Bootstrap CDN -->
    <link rel="stylesheet" href="{{ asset('css/login.css') }}"> <!-- Update this path -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome CDN -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css" rel="stylesheet">
    <style>
        body, html {
            height: 100%;
            margin: 0;
            font-family: 'Arial', sans-serif;
            background-color: #f4f7fa;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .password-card {
            background-color: #fff;
            padding: 2rem;
            border-radius: 20px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 350px;
        }

        .password-card .logo {
            max-width: 150px;
            margin-bottom: 2rem;
        }

        .password-card form .form-group {
            margin-bottom: 2rem;
            position: relative;
        }

        .password-card form .form-control {
            padding-left: 40px;
            padding-top: 10px;
            padding-bottom: 10px;
        }

        .form-icon {
            position: absolute;
            left: 12px;
            top: 50%;
            transform: translateY(-50%);
            color: #495057;
        }

        .password-card form .btn {
            width: 100%;
            background-color: #007bff;
            color: #fff;
            border-radius: 5px;
            padding: 10px;
            border: none;
            cursor: pointer;
        }

        .password-card form .btn:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
<div class="screen screen-1">
<div class="password-card">
        <img src="https://img.hotimg.com/logoe0577f94706fddae.png" alt="Logo" class="logo">
    <h4 class="text-center mb-4"><i class="fas fa-lock"></i> Change Password</h4>
    {{ form_start(resetForm, {'attr': {'class': 'form-horizontal'}}) }}
    <div class="form-group">
        <i class="fas fa-key form-icon"></i>
        {{ form_widget(resetForm.plainPassword.first, {'attr': {'class': 'form-control', 'placeholder': 'New password'}}) }}
		  {{ form_errors(resetForm.plainPassword.first) }}
    </div>
    <div class="form-group">
        <i class="fas fa-redo form-icon"></i>
        {{ form_widget(resetForm.plainPassword.second, {'attr': {'class': 'form-control', 'placeholder': 'Confirm Password'}}) }}
		  {{ form_errors(resetForm.plainPassword.second) }}
    </div>
    <button type="submit" class="btn btn-primary">Change</button>
    {{ form_end(resetForm) }}
</div>
</div>

</body>
</html>
